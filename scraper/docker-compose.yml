services:
  khoral-scrap-api:
    build: ./api
    container_name: khoral-scrap-api
    restart: unless-stopped
    volumes:
      - ./saidas/saidas:/data/saidas:ro
    environment:
      - SCRAPER_OUTPUT_DIR=/data/saidas
    networks:
      - web

networks:
  web:
    external: true
